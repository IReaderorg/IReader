package eu.kanade.tachiyomi.source

/**
 * PreferenceScreen — extensions call setupPreferenceScreen(screen).
 * On Android, this could be backed by real AndroidX PreferenceScreen,
 * but for the source-api module we use a simple class.
 */
class PreferenceScreen
