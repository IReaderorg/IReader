// Cheerio placeholder - In production, this should be replaced with a proper cheerio build
// For LNReader plugins, we'll use jsoup on the Kotlin side instead
// This file is a placeholder to maintain the expected structure
(function() {
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = {
      load: function(html) {
        // This will be implemented by the JSLibraryProvider using jsoup
        throw new Error('Cheerio must be provided by the native bridge');
      }
    };
  }
})();
