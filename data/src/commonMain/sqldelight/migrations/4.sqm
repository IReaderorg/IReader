-- Migration to add customFonts table

CREATE TABLE IF NOT EXISTS customFonts(
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    filePath TEXT NOT NULL,
    isSystemFont INTEGER NOT NULL DEFAULT 0,
    dateAdded INTEGER NOT NULL
);
