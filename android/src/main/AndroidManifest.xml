<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    <!-- Internet permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- For background jobs -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- For managing extensions -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />
    
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.android.launcher.permission.INSTALL_SHORTCUT" />

    <!-- To view extension packages in API 30+ -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    
    <!-- For loading extension APKs from cache -->
    <uses-permission android:name="android.permission.READ_INSTALL_SESSIONS" />
    
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>

        <!-- Add other extension package names as needed -->
        <!-- Also add a generic intent filter for package visibility -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="ireader.*" />
        </intent>
        

    </queries>

    <application
        android:name="org.ireader.app.MyApplication"
        android:allowBackup="true"
        android:configChanges="orientation|screenSize|screenLayout"
        android:fullBackupOnly="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="IReader"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:screenOrientation="fullSensor"
        android:supportsRtl="true"
        android:theme="@style/Theme.Splash"
        tools:targetApi="33">
        <activity
            android:name="org.ireader.app.crash.CrashActivity"
            android:exported="false"
            android:theme="@style/Theme.Crash"
            android:process=":crash" />
        
        <activity
            android:name="org.ireader.app.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- App Shortcuts Menu -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
            
            <intent-filter>
                <data
                    android:host="www.ireader.org"
                    android:scheme="https" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="deeplink"
                    android:scheme="ireader" />
            </intent-filter>
            
            <!-- AniList OAuth callback -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="anilist-auth"
                    android:scheme="ireader" />
            </intent-filter>
            
            <!-- Google Drive OAuth is handled via Google Sign-In SDK, no intent filter needed -->


        </activity>
        <activity
            android:name="ireader.presentation.core.ui.UnlockActivity"
            android:exported="false"
            android:theme="@style/Theme.Splash" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                tools:node="remove" />
            <!-- Ensure ProfileInstaller is initialized for baseline profiles -->
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup"
                tools:node="merge" />
        </provider>

        <!-- TTS Service - Clean architecture background service -->
        <service
            android:name="ireader.domain.services.tts_service.v2.TTSV2Service"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

        <!-- App Update Download Service -->
        <service
            android:name="ireader.domain.services.update_service.AppUpdateDownloadService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- WorkManager foreground service for long-running downloads -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <receiver
            android:name="androidx.media.session.MediaButtonReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <meta-data
            android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="false" />
        <meta-data
            android:name="android.webkit.WebView.MetricsOptOut"
            android:value="true" />

        <!-- ProfileInstaller receiver for baseline profiles and benchmark support -->
        <!-- Explicitly declare to ensure it's not removed by manifest merger -->
        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:exported="true"
            tools:node="merge">
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
        
        <!-- Download action receiver for pause/resume/cancel and other actions -->
        <receiver
            android:name="ireader.domain.services.downloaderService.DownloadActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ireader.action.DOWNLOAD_PAUSE" />
                <action android:name="ireader.action.DOWNLOAD_RESUME" />
                <action android:name="ireader.action.DOWNLOAD_CANCEL" />
                <action android:name="ireader.action.DOWNLOAD_ALLOW_MOBILE" />
                <action android:name="ireader.action.DOWNLOAD_RETRY_ALL" />
                <action android:name="ireader.action.DOWNLOAD_CLEAR_FAILED" />
            </intent-filter>
        </receiver>
        
        <!-- TTS Download action receiver for pause/cancel -->
        <receiver
            android:name="ireader.domain.services.tts_service.TTSDownloadActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ireader.tts.download.PAUSE" />
                <action android:name="ireader.tts.download.RESUME" />
                <action android:name="ireader.tts.download.TOGGLE_PAUSE" />
                <action android:name="ireader.tts.download.CANCEL" />
            </intent-filter>
        </receiver>
        
        <!-- TTS V2 actions are handled by TTSV2Service's internal receiver -->
    </application>

</manifest>
